---
wave: 3
depends_on: [03-05]
files_modified:
  - src/popup/components/PromptEditDialog.tsx
  - src/popup/components/DeleteConfirmDialog.tsx
  - src/popup/App.tsx
autonomous: true
---

# 03-06: Prompt CRUD Dialogs

## Requirements
- MGMT-02: 用户可新增提示词（内容、所属分类）
- MGMT-03: 用户可编辑已有提示词内容
- MGMT-04: 用户可删除提示词

## Summary
Implement Prompt edit/add dialog and delete confirmation dialog using shadcn AlertDialog. Edit dialog includes name input, content textarea, and category dropdown. Delete dialog shows confirmation per UI-SPEC copywriting.

## Tasks

### Task 1: Create PromptEditDialog Component
<read_first>
- .planning/phases/03-data-management-popup-ui/03-UI-SPEC.md: Components needed (input, textarea, button)
- .planning/phases/03-data-management-popup-ui/03-CONTEXT.md: D-05 decision for dialog fields
- src/lib/store.ts: usePromptStore with categories, addPrompt, updatePrompt
- src/popup/components/ui/button.tsx: Button component pattern
</read_first>

<action>
Create `src/popup/components/PromptEditDialog.tsx`:

1. Add shadcn components:
   - Run: `npx shadcn@latest add dialog input textarea select`

2. Dialog structure (per D-05):
   - Name: Input field (required)
   - Content: Textarea (required)
   - Category: Select dropdown with all categories
   - Footer: "保存" (Save) + "取消" (Cancel) buttons

3. Mode handling:
   - If prompt prop is null → Add mode (generate new ID)
   - If prompt prop exists → Edit mode (update existing)

4. Implementation:
   ```typescript
   import { useState, useEffect } from 'react'
   import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from './ui/dialog'
   import { Button } from './ui/button'
   import { Input } from './ui/input'
   import { Textarea } from './ui/textarea'
   import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from './ui/select'
   import { usePromptStore } from '../../lib/store'
   import type { Prompt } from '../../shared/types'
   import { v4 as uuidv4 } from 'uuid'

   interface PromptEditDialogProps {
     prompt: Prompt | null  // null = add mode
     open: boolean
     onClose: () => void
   }

   function PromptEditDialog({ prompt, open, onClose }: PromptEditDialogProps) {
     const { categories, addPrompt, updatePrompt, selectedCategoryId } = usePromptStore()
     
     const [name, setName] = useState('')
     const [content, setContent] = useState('')
     const [categoryId, setCategoryId] = useState(selectedCategoryId || 'default')

     useEffect(() => {
       if (prompt) {
         setName(prompt.name)
         setContent(prompt.content)
         setCategoryId(prompt.categoryId)
       } else {
         setName('')
         setContent('')
         setCategoryId(selectedCategoryId || 'default')
       }
     }, [prompt, open, selectedCategoryId])

     const handleSave = () => {
       if (!name.trim() || !content.trim()) return

       if (prompt) {
         updatePrompt(prompt.id, { name, content, categoryId })
       } else {
         addPrompt({ name, content, categoryId })
       }
       onClose()
     }

     return (
       <Dialog open={open} onOpenChange={onClose}>
         <DialogContent className="w-[280px]">
           <DialogHeader>
             <DialogTitle>{prompt ? '编辑提示词' : '添加提示词'}</DialogTitle>
           </DialogHeader>
           <div className="flex flex-col gap-4">
             <div>
               <label className="text-xs text-muted-foreground mb-1">名称</label>
               <Input value={name} onChange={e => setName(e.target.value)} placeholder="提示词名称" />
             </div>
             <div>
               <label className="text-xs text-muted-foreground mb-1">内容</label>
               <Textarea value={content} onChange={e => setContent(e.target.value)} placeholder="提示词内容" rows={4} />
             </div>
             <div>
               <label className="text-xs text-muted-foreground mb-1">所属分类</label>
               <Select value={categoryId} onValueChange={setCategoryId}>
                 <SelectTrigger>
                   <SelectValue />
                 </SelectTrigger>
                 <SelectContent>
                   {categories.map(cat => (
                     <SelectItem key={cat.id} value={cat.id}>{cat.name}</SelectItem>
                   ))}
                 </SelectContent>
               </Select>
             </div>
           </div>
           <DialogFooter>
             <Button variant="outline" onClick={onClose}>取消</Button>
             <Button onClick={handleSave}>保存</Button>
           </DialogFooter>
         </DialogContent>
       </Dialog>
     )
   }
   ```
</action>

<acceptance_criteria>
- src/popup/components/PromptEditDialog.tsx exists
- Component imports Dialog components from shadcn/ui
- Component imports Input, Textarea, Select from shadcn/ui
- Dialog has name Input, content Textarea, category Select
- Dialog title shows "编辑提示词" or "添加提示词"
- Dialog has "取消" and "保存" buttons
- handleSave calls addPrompt or updatePrompt based on mode
- New prompts default to selectedCategoryId or 'default'
- TypeScript compilation passes
</acceptance_criteria>

### Task 2: Create DeleteConfirmDialog Component
<read_first>
- .planning/phases/03-data-management-popup-ui/03-UI-SPEC.md: Copywriting contract for delete confirmation
- src/popup/components/ui/alert-dialog.tsx: AlertDialog from shadcn
- src/lib/store.ts: usePromptStore with deletePrompt
</read_first>

<action>
Create `src/popup/components/DeleteConfirmDialog.tsx`:

1. Add AlertDialog component:
   - Run: `npx shadcn@latest add alert-dialog`

2. Dialog structure (per UI-SPEC):
   - Title: "确定删除此提示词？"
   - Description: Optional context
   - Actions: "取消" (Cancel) + "删除" (Delete - destructive style)

3. Implementation:
   ```typescript
   import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogHeader, AlertDialogTitle } from './ui/alert-dialog'
   import { Button } from './ui/button'

   interface DeleteConfirmDialogProps {
     open: boolean
     onClose: () => void
     onConfirm: () => void
     itemName: string  // Prompt or category name
     description?: string
   }

   function DeleteConfirmDialog({ open, onClose, onConfirm, itemName, description }: DeleteConfirmDialogProps) {
     return (
       <AlertDialog open={open} onOpenChange={onClose}>
         <AlertDialogContent className="w-[280px]">
           <AlertDialogHeader>
             <AlertDialogTitle>确定删除{itemName}？</AlertDialogTitle>
             {description && (
               <AlertDialogDescription>{description}</AlertDialogDescription>
             )}
           </AlertDialogHeader>
           <div className="flex justify-end gap-2">
             <AlertDialogCancel>取消</AlertDialogCancel>
             <AlertDialogAction onClick={onConfirm} className="bg-destructive text-destructive-foreground hover:bg-destructive/90">
               删除
             </AlertDialogAction>
           </div>
         </AlertDialogContent>
       </AlertDialog>
     )
   }
   ```
</action>

<acceptance_criteria>
- src/popup/components/DeleteConfirmDialog.tsx exists
- Component imports AlertDialog components from shadcn/ui
- Title shows "确定删除{itemName}？"
- Cancel button shows "取消"
- Confirm button shows "删除" with destructive styling
- onConfirm callback triggered on delete action
- TypeScript compilation passes
</acceptance_criteria>

### Task 3: Add Bottom "添加提示词" Button
<read_first>
- .planning/phases/03-data-management-popup-ui/03-CONTEXT.md: D-13 decision for fixed bottom button
- .planning/phases/03-data-management-popup-ui/03-UI-SPEC.md: Visual hierarchy for CTA button
- src/popup/App.tsx: Current layout structure
</read_first>

<action>
Update `src/popup/App.tsx`:

1. Add fixed bottom button (per D-13):
   - Button always visible at bottom of popup
   - Primary CTA style (bg-primary)
   - Opens PromptEditDialog in add mode

2. Update App to include dialogs:
   ```typescript
   import PromptEditDialog from './components/PromptEditDialog'
   import DeleteConfirmDialog from './components/DeleteConfirmDialog'
   
   // Add state:
   const [editDialogOpen, setEditDialogOpen] = useState(false)
   const [deleteDialogOpen, setDeleteDialogOpen] = useState(false)
   const [promptToDelete, setPromptToDelete] = useState<{ id: string, name: string } | null>(null)

   // Update handleDeletePrompt:
   const handleDeletePrompt = (id: string) => {
     const prompt = prompts.find(p => p.id === id)
     if (prompt) {
       setPromptToDelete({ id, name: prompt.name })
       setDeleteDialogOpen(true)
     }
   }

   const confirmDelete = () => {
     if (promptToDelete) {
       deletePrompt(promptToDelete.id)
       setPromptToDelete(null)
       setDeleteDialogOpen(false)
     }
   }

   // In layout, after flex content area:
   <div className="p-4 border-t border-border">
     <Button onClick={() => { setEditingPrompt(null); setEditDialogOpen(true) }} className="w-full">
       添加提示词
     </Button>
   </div>

   // Dialogs at end of component:
   <PromptEditDialog 
     prompt={editingPrompt}
     open={editDialogOpen}
     onClose={() => setEditDialogOpen(false)}
   />
   <DeleteConfirmDialog
     open={deleteDialogOpen}
     onClose={() => setDeleteDialogOpen(false)}
     onConfirm={confirmDelete}
     itemName={promptToDelete?.name || ''}
   />
   ```

3. Update PromptList click handlers:
   - onEditPrompt: set editingPrompt, open editDialogOpen
   - onDeletePrompt: set promptToDelete, open deleteDialogOpen
</action>

<acceptance_criteria>
- App imports PromptEditDialog and DeleteConfirmDialog
- App has editDialogOpen, deleteDialogOpen state
- Bottom "添加提示词" button exists with w-full, primary style
- Button opens PromptEditDialog with null prompt (add mode)
- Delete shows confirmation dialog before calling deletePrompt
- PromptEditDialog receives editingPrompt from PromptList
- TypeScript compilation passes
</acceptance_criteria>

## Verification
1. "添加提示词" button opens add dialog
2. Add dialog saves new prompt with name, content, category
3. Edit menu opens edit dialog with existing prompt data
4. Delete menu shows confirmation before deleting
5. All changes persist to storage

## must_haves
- PromptEditDialog for add/edit with name, content, category fields
- DeleteConfirmDialog with confirmation UI
- Fixed bottom "添加提示词" button (per D-13)
- Delete requires confirmation (per UI-SPEC)
- Storage persistence after each operation