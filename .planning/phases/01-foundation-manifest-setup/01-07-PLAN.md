---
wave: 2
depends_on: [01, 02]
files_modified:
  - src/popup/popup.html
  - src/popup/popup.tsx
  - src/popup/App.tsx
  - src/popup/index.css
autonomous: true
requirements_addressed: [EXT-04]
---

# Plan 07: Popup Skeleton

**Objective:** Create minimal Popup UI with React that opens when extension icon is clicked.

---

## Task 01-07-01: Create popup.html

<read_first>
- No existing file (new project)
- .planning/RESEARCH.md section 2 for project structure
</read_first>

<action>
Create `src/popup/popup.html` with:

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lovart Prompt Injector</title>
  <link rel="stylesheet" href="./index.css">
</head>
<body>
  <div id="root"></div>
  <script type="module" src="./popup.tsx"></script>
</body>
</html>
```
</action>

<acceptance_criteria>
- src/popup/popup.html exists
- File contains `<div id="root"></div>`
- File references `./index.css` stylesheet
- File references `./popup.tsx` script with type="module"
</acceptance_criteria>

---

## Task 01-07-02: Create popup.tsx (React entry)

<read_first>
- No existing file (new project)
- React entry point pattern
</read_first>

<action>
Create `src/popup/popup.tsx` with:

```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```
</action>

<acceptance_criteria>
- src/popup/popup.tsx exists
- File imports React, ReactDOM, App, and index.css
- File contains `ReactDOM.createRoot` call
- File renders `<App />` inside `<React.StrictMode>`
</acceptance_criteria>

---

## Task 01-07-03: Create App.tsx (Root component)

<read_first>
- No existing file (new project)
</read_first>

<action>
Create `src/popup/App.tsx` with placeholder UI:

```typescript
import React from 'react'

function App() {
  return (
    <div className="popup-container">
      <h1>Lovart Prompt Injector</h1>
      <p>Phase 1: 扩展骨架已建立</p>
      <p>Phase 2: Lovart集成</p>
      <p>Phase 3: 数据管理</p>
    </div>
  )
}

export default App
```
</action>

<acceptance_criteria>
- src/popup/App.tsx exists
- File exports React component named `App`
- Component returns valid JSX with `<div className="popup-container">`
- Component displays "Lovart Prompt Injector" title
</acceptance_criteria>

---

## Task 01-07-04: Create index.css (Popup styles)

<read_first>
- No existing file (new project)
</read_first>

<action>
Create `src/popup/index.css` with minimal popup styles:

```css
:root {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  font-size: 14px;
}

body {
  margin: 0;
  padding: 12px;
  width: 300px;
  min-height: 200px;
}

.popup-container {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

h1 {
  font-size: 16px;
  font-weight: 600;
  color: #333;
  margin: 0;
}

p {
  color: #666;
  margin: 0;
}
```
</action>

<acceptance_criteria>
- src/popup/index.css exists
- File contains `body` selector with width: 300px
- File contains `.popup-container` class
- File contains `h1` and `p` styles
</acceptance_criteria>

---

## Verification

<manual>
After completing all tasks and loading extension:
1. Click extension icon in Chrome toolbar
2. Verify: Popup window opens
3. Verify: Popup displays "Lovart Prompt Injector" title
4. Verify: Popup shows Phase progress text
5. Verify: Popup has 300px width (per CSS)
</manual>

---

## Threat Model

Not applicable — static UI with no sensitive data display.

---

*Plan 07 for Phase 01-foundation-manifest-setup*