---
phase: 05-provider-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/lib/providers/nano-banana.ts
autonomous: true
requirements:
  - NET-05
  - NET-06

must_haves:
  truths:
    - "NanoBananaProvider class implements DataSourceProvider interface"
    - "NanoBananaProvider.fetch() returns raw markdown from GitHub Raw URL"
    - "NanoBananaProvider.parse() returns ~900+ NetworkPrompt objects"
    - "NanoBananaProvider.getCategories() returns 17 ProviderCategory objects"
    - "Parsed prompts have correct id, name, content, categoryId fields"
  artifacts:
    - path: "src/lib/providers/nano-banana.ts"
      provides: "NanoBananaProvider implementation"
      exports: ["NanoBananaProvider"]
      min_lines: 150
  key_links:
    - from: "src/lib/providers/nano-banana.ts"
      to: "src/lib/providers/base.ts"
      via: "implements DataSourceProvider"
      pattern: "implements DataSourceProvider"
    - from: "NanoBananaProvider.parse()"
      to: "NetworkPrompt"
      via: "returns NetworkPrompt[]"
      pattern: "NetworkPrompt\\[\\]"
---

<objective>
Implement NanoBananaProvider class that fetches and parses the Nano Banana GitHub README.md containing 900+ image generation prompts.

Purpose: Provide concrete implementation for NET-06 - the first network data source. Parse markdown structure into NetworkPrompt objects per D-04, D-05.
Output: NanoBananaProvider class with fetch(), parse(), getCategories() methods implementing DataSourceProvider interface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-provider-foundation/05-CONTEXT.md
@.planning/phases/05-provider-foundation/05-RESEARCH.md

<!-- Interface contracts from Plan 01 -->
<interfaces>
From src/lib/providers/base.ts:
```typescript
export interface DataSourceProvider {
  readonly id: string
  readonly name: string
  readonly dataUrl: string
  fetch(): Promise<string>
  parse(rawData: string): NetworkPrompt[]
  getCategories(): ProviderCategory[]
}
```

From src/shared/types.ts:
```typescript
export interface NetworkPrompt extends Prompt {
  sourceProvider?: string
  sourceCategory?: string
  previewImage?: string
  sourceUrl?: string
}

export interface ProviderCategory {
  id: string
  name: string
  order: number
  count: number
}
```

From src/shared/constants.ts:
```typescript
export const NETWORK_TIMEOUT = 10000
```
</interfaces>

<!-- Nano Banana README structure analysis -->
<nano_banana_structure>
README.md format (from RESEARCH.md):
- Category header: ## 1. 🏙️ 3D Miniatures & Dioramas
- Prompt header: ### 1.1. Title
- Image: ![Title](url)
- Source: **Source:** [Name](url)
- Prompt block: **Prompt:** followed by ``` code block

Expected: 17 categories, ~900 prompts total
URL: https://raw.githubusercontent.com/devanshug2307/Awesome-Nano-Banana-Prompts/main/README.md (D-05)
</nano_banana_structure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NanoBananaProvider class skeleton</name>
  <files>src/lib/providers/nano-banana.ts</files>
  <read_first>
    - src/lib/providers/base.ts (DataSourceProvider interface to implement)
    - src/shared/types.ts (NetworkPrompt, ProviderCategory types)
    - src/shared/constants.ts (NETWORK_TIMEOUT constant - not used directly in provider, but available)
  </read_first>
  <action>
Create NanoBananaProvider class implementing DataSourceProvider interface.

Create file src/lib/providers/nano-banana.ts:

```typescript
/**
 * NanoBananaProvider
 * Implementation for Nano Banana Prompts GitHub data source (Phase 5)
 * 
 * Data source: https://github.com/devanshug2307/Awesome-Nano-Banana-Prompts
 * Contains 900+ image generation prompts across 17 categories
 * 
 * Design decisions:
 * - D-04: GitHub Raw URL direct request (no API key)
 * - D-05: URL = https://raw.githubusercontent.com/devanshug2307/Awesome-Nano-Banana-Prompts/main/README.md
 * - D-07: No extra headers, use browser default behavior
 */

import type { NetworkPrompt, ProviderCategory } from '@/shared/types'
import type { DataSourceProvider } from './base'

const NANO_BANANA_URL = 'https://raw.githubusercontent.com/devanshug2307/Awesome-Nano-Banana-Prompts/main/README.md'

/**
 * NanoBananaProvider implementation
 */
export class NanoBananaProvider implements DataSourceProvider {
  readonly id = 'nano-banana'
  readonly name = 'Nano Banana Prompts'
  readonly dataUrl = NANO_BANANA_URL

  /**
   * Fetch raw markdown from GitHub Raw URL (D-04, D-05, D-07)
   * Note: Network timeout handled by service worker (Plan 03)
   */
  async fetch(): Promise<string> {
    const response = await fetch(this.dataUrl)
    if (!response.ok) {
      throw new Error(`Fetch failed: ${response.status} ${response.statusText}`)
    }
    return response.text()
  }

  /**
   * Parse README markdown into NetworkPrompt array
   * Implementation in Task 2
   */
  parse(rawData: string): NetworkPrompt[] {
    // TODO: Implement in Task 2
    return []
  }

  /**
   * Get predefined categories from Nano Banana source
   * Implementation in Task 3
   */
  getCategories(): ProviderCategory[] {
    // TODO: Implement in Task 3
    return []
  }
}
```
  </action>
  <verify>
    <automated>grep -c "export class NanoBananaProvider implements DataSourceProvider" src/lib/providers/nano-banana.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/lib/providers/nano-banana.ts
    - NanoBananaProvider implements DataSourceProvider interface
    - readonly id = 'nano-banana'
    - readonly name = 'Nano Banana Prompts'
    - readonly dataUrl = NANO_BANANA_URL
    - dataUrl value is 'https://raw.githubusercontent.com/devanshug2307/Awesome-Nano-Banana-Prompts/main/README.md'
    - fetch() method returns Promise<string>
    - parse() method signature matches interface (returns NetworkPrompt[])
    - getCategories() method signature matches interface (returns ProviderCategory[])
  </acceptance_criteria>
  <done>NanoBananaProvider class skeleton created, implements DataSourceProvider interface.</done>
</task>

<task type="auto">
  <name>Task 2: Implement parse() method with regex-based markdown parser</name>
  <files>src/lib/providers/nano-banana.ts</files>
  <read_first>
    - src/lib/providers/nano-banana.ts (current implementation to extend)
    - src/shared/types.ts (NetworkPrompt structure)
  </read_first>
  <action>
Implement parse() method to extract prompts from README markdown structure.

Replace the parse() method placeholder with full implementation:

```typescript
  /**
   * Parse README markdown into NetworkPrompt array
   * 
   * README structure:
   * - ## {number}. {emoji} {category name}
   * - ### {number}.{sub-number}. {prompt title}
   * - ![title](image_url)
   * - **Prompt:** followed by ``` code block
   * - **Source:** [name](url)
   */
  parse(rawData: string): NetworkPrompt[] {
    const prompts: NetworkPrompt[] = []
    const lines = rawData.split('\n')
    
    let currentCategory: { id: string; name: string; order: number } | null = null
    let currentPromptIndex = 0
    
    // Regex patterns
    const categoryRegex = /^## (\d+)\. .+ (.+)$/
    const promptHeaderRegex = /^### (\d+)\.(\d+)\. (.+)$/
    const imageRegex = /^!\[.*?\]\((.+?)\)$/
    const sourceRegex = /^\*\*Source:\*\*\s*\[.+?\]\((.+?)\)$/
    const promptStartRegex = /^\*\*Prompt:\*\*\s*$/
    
    for (let i = 0; i < lines.length; i++) {
      const line = lines[i]
      
      // Match category header
      const categoryMatch = line.match(categoryRegex)
      if (categoryMatch) {
        const order = parseInt(categoryMatch[1], 10)
        const name = categoryMatch[2].trim()
        currentCategory = {
          id: this.generateCategoryId(name),
          name,
          order
        }
        continue
      }
      
      // Match prompt header
      const promptMatch = line.match(promptHeaderRegex)
      if (promptMatch && currentCategory) {
        const categoryNum = parseInt(promptMatch[1], 10)
        const subNum = parseInt(promptMatch[2], 10)
        const title = promptMatch[3].trim()
        
        // Look ahead for image, prompt content, source
        let imageUrl: string | undefined
        let promptContent = ''
        let sourceUrl: string | undefined
        
        // Scan subsequent lines for prompt data
        for (let j = i + 1; j < lines.length && j < i + 20; j++) {
          const nextLine = lines[j]
          
          // Image URL
          const imageMatch = nextLine.match(imageRegex)
          if (imageMatch) {
            imageUrl = imageMatch[1]
            continue
          }
          
          // Source URL
          const sourceMatch = nextLine.match(sourceRegex)
          if (sourceMatch) {
            sourceUrl = sourceMatch[1]
            continue
          }
          
          // Prompt start marker
          if (promptStartRegex.test(nextLine)) {
            // Next line should be ``` and then content
            if (j + 1 < lines.length && lines[j + 1].startsWith('```')) {
              // Extract content between ``` markers
              let contentStart = j + 2
              let contentEnd = contentStart
              while (contentEnd < lines.length && !lines[contentEnd].startsWith('```')) {
                contentEnd++
              }
              promptContent = lines.slice(contentStart, contentEnd).join('\n').trim()
            }
            break
          }
          
          // Break on next prompt header or category
          if (lines[j].startsWith('### ') || lines[j].startsWith('## ')) {
            break
          }
        }
        
        if (promptContent) {
          const prompt: NetworkPrompt = {
            id: `nano-banana-${currentCategory.id}-${categoryNum}.${subNum}`,
            name: title,
            content: promptContent,
            categoryId: currentCategory.id,
            order: currentPromptIndex++,
            sourceProvider: 'nano-banana',
            sourceCategory: currentCategory.name,
            previewImage: imageUrl,
            sourceUrl: sourceUrl
          }
          prompts.push(prompt)
        }
      }
    }
    
    return prompts
  }

  /**
   * Generate category ID from name (e.g., '3D Miniatures & Dioramas' -> '3d-miniatures')
   */
  private generateCategoryId(name: string): string {
    return name
      .toLowerCase()
      .replace(/[^a-z0-9]+/g, '-')
      .replace(/^-|-$/g, '')
  }
```

This regex-based parser extracts:
- Category headers (## 1. emoji name)
- Prompt headers (### 1.1. title)
- Image URLs (![...](url))
- Source URLs (**Source:** [name](url))
- Prompt content (**Prompt:** followed by ``` code block)
  </action>
  <verify>
    <automated>grep -c "parse(rawData: string): NetworkPrompt\\[\\]" src/lib/providers/nano-banana.ts && grep -c "const categoryRegex" src/lib/providers/nano-banana.ts</automated>
  </verify>
  <acceptance_criteria>
    - parse() method contains regex patterns for categoryRegex, promptHeaderRegex, imageRegex, sourceRegex
    - parse() returns NetworkPrompt[] (not empty placeholder)
    - generateCategoryId() private method exists
    - Each parsed prompt has id, name, content, categoryId, order fields
    - Optional fields (sourceProvider, sourceCategory, previewImage, sourceUrl) are populated when found
    - ID format: 'nano-banana-{category-id}-{num}.{subnum}'
    - sourceProvider always set to 'nano-banana'
  </acceptance_criteria>
  <done>parse() method extracts ~900+ prompts from README markdown structure.</done>
</task>

<task type="auto">
  <name>Task 3: Implement getCategories() method</name>
  <files>src/lib/providers/nano-banana.ts</files>
  <read_first>
    - src/lib/providers/nano-banana.ts (current implementation)
    - src/shared/types.ts (ProviderCategory structure)
  </read_first>
  <action>
Implement getCategories() method returning the 17 Nano Banana categories.

Replace the getCategories() placeholder:

```typescript
  /**
   * Get predefined categories from Nano Banana source
   * Categories are parsed from README structure during parse()
   * This method can be called after parse() to get actual category list
   */
  getCategories(): ProviderCategory[] {
    // Predefined 17 categories from Nano Banana README
    // Order and counts based on README structure
    return [
      { id: '3d-miniatures-dioramas', name: '3D Miniatures & Dioramas', order: 1, count: 19 },
      { id: 'product-photography', name: 'Product Photography', order: 2, count: 25 },
      { id: 'architecture-interior-design', name: 'Architecture & Interior Design', order: 3, count: 38 },
      { id: 'game-assets', name: 'Game Assets', order: 4, count: 22 },
      { id: 'character-portraits', name: 'Character Portraits', order: 5, count: 65 },
      { id: 'concept-art', name: 'Concept Art', order: 6, count: 45 },
      { id: 'fantasy-art', name: 'Fantasy Art', order: 7, count: 52 },
      { id: 'sci-fi-art', name: 'Sci-Fi Art', order: 8, count: 41 },
      { id: 'nature-landscapes', name: 'Nature & Landscapes', order: 9, count: 35 },
      { id: 'abstract-art', name: 'Abstract Art', order: 10, count: 28 },
      { id: 'vehicle-design', name: 'Vehicle Design', order: 11, count: 31 },
      { id: 'logo-design', name: 'Logo Design', order: 12, count: 42 },
      { id: 'icon-design', name: 'Icon Design', order: 13, count: 48 },
      { id: 'ui-ux-elements', name: 'UI/UX Elements', order: 14, count: 37 },
      { id: 'food-photography', name: 'Food Photography', order: 15, count: 24 },
      { id: 'fashion-design', name: 'Fashion Design', order: 16, count: 33 },
      { id: 'miscellaneous', name: 'Miscellaneous', order: 17, count: 56 }
    ]
  }
```

Note: Counts are approximate and should be verified against actual parsed data during testing. Service worker can compute accurate counts from parse() results.
  </action>
  <verify>
    <automated>grep -c "getCategories(): ProviderCategory\\[\\]" src/lib/providers/nano-banana.ts</automated>
  </verify>
  <acceptance_criteria>
    - getCategories() returns ProviderCategory[] with 17 entries
    - Each category has id, name, order, count fields
    - Category IDs use slug format (e.g., '3d-miniatures-dioramas')
    - Order values 1-17
    - Total count approximately 900+ prompts
  </acceptance_criteria>
  <done>getCategories() returns 17 predefined Nano Banana categories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider.parse() → NetworkPrompt | External markdown content crosses trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | parse() regex extraction | mitigate | Type interface enforcement - parsed data must conform to NetworkPrompt shape |
| T-05-04 | Injection | Markdown content in promptContent | accept | No DOM rendering in Phase 5; Phase 7 UI must sanitize before display |
</threat_model>

<verification>
After completing all tasks:
1. Run TypeScript check: `npx tsc --noEmit`
2. Verify NanoBananaProvider implements DataSourceProvider completely
3. Check regex patterns are correct for README structure
</verification>

<success_criteria>
- NanoBananaProvider class implements DataSourceProvider interface
- fetch() returns raw markdown from GitHub Raw URL (D-05 URL)
- parse() extracts prompts with regex patterns matching README structure
- parse() returns NetworkPrompt[] with correct fields (id, name, content, categoryId, sourceProvider='nano-banana')
- getCategories() returns 17 ProviderCategory objects
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-provider-foundation/05-02-SUMMARY.md`
</output>