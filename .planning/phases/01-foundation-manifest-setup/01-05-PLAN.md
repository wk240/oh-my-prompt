---
wave: 2
depends_on: [01, 02, 04]
files_modified:
  - src/background/service-worker.ts
autonomous: true
requirements_addressed: []
---

# Plan 05: Service Worker Skeleton

**Objective:** Create Service Worker with message routing infrastructure for PING response and future storage/prompt operations.

---

## Task 01-05-01: Create Service Worker entry

<read_first>
- No existing file (new project)
- .planning/RESEARCH.md section 4 for Service Worker message handler
- src/shared/messages.ts (created in Plan 04)
</read_first>

<action>
Create `src/background/service-worker.ts` with exact content:

```typescript
import { MessageType, MessageResponse } from '../shared/messages'

console.log('[Lovart Injector] Service Worker started')

chrome.runtime.onMessage.addListener(
  (message, sender, sendResponse) => {
    console.log('[Lovart Injector] Received message:', message.type)

    switch (message.type) {
      case MessageType.PING:
        sendResponse({ success: true, data: 'pong' } as MessageResponse<string>)
        break

      // Future handlers (Phase 2-3)
      case MessageType.GET_STORAGE:
        // Phase 3: Implement storage retrieval
        sendResponse({ success: false, error: 'GET_STORAGE not implemented' })
        break

      case MessageType.SET_STORAGE:
        // Phase 3: Implement storage save
        sendResponse({ success: false, error: 'SET_STORAGE not implemented' })
        break

      case MessageType.INSERT_PROMPT:
        // Phase 2: Implement prompt insertion
        sendResponse({ success: false, error: 'INSERT_PROMPT not implemented' })
        break

      default:
        sendResponse({ success: false, error: `Unknown message type: ${message.type}` })
    }

    return true // Required for async sendResponse
  }
)
```
</action>

<acceptance_criteria>
- src/background/service-worker.ts exists
- File imports MessageType and MessageResponse from '../shared/messages'
- File contains `chrome.runtime.onMessage.addListener`
- File handles MessageType.PING with `{ success: true, data: 'pong' }` response
- File returns `true` from listener for async response
</acceptance_criteria>

---

## Verification

<manual>
After completing task and loading extension:
1. Open chrome://extensions
2. Click "Service Worker" link in extension details
3. In DevTools Console, verify: `[Lovart Injector] Service Worker started` log
4. Navigate to lovart.ai (or any page)
5. In page DevTools Console, run: `chrome.runtime.sendMessage({type: 'PING'}, console.log)`
6. Verify: Response `{success: true, data: 'pong'}` in console
</manual>

---

## Threat Model

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Message injection from untrusted content | LOW | Content script only runs on lovart.ai (D-01), trusted origin |
| Unhandled message types | LOW | Default case returns error, prevents silent failures |
</threat_model>

---

*Plan 05 for Phase 01-foundation-manifest-setup*