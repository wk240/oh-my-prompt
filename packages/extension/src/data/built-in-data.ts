/**
 * Built-in prompt data for first-time initialization
 * Curated for Lovart AI design/illustration platform
 * Bilingual support: Chinese (content) + English (contentEn)
 */

import type { Prompt, Category } from '@oh-my-prompt/shared/types'

/**
 * Built-in categories (empty for fresh install)
 */
export const BUILT_IN_CATEGORIES: Category[] = []

/**
 * Built-in prompts (empty for fresh install)
 */
export const BUILT_IN_PROMPTS: Prompt[] = []