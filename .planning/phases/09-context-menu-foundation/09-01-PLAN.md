---
phase: 09-context-menu-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [manifest.json, src/shared/constants.ts]
autonomous: true
requirements: [MENU-01]
user_setup: []

must_haves:
  truths:
    - "Extension has contextMenus permission enabled"
    - "Storage key constant exists for captured image URL"
  artifacts:
    - path: "manifest.json"
      provides: "Chrome API permission"
      contains: '"contextMenus"'
    - path: "src/shared/constants.ts"
      provides: "Storage key for captured URLs"
      contains: "CAPTURED_IMAGE_STORAGE_KEY"
  key_links:
    - from: "manifest.json permissions"
      to: "Chrome contextMenus API"
      via: "permission declaration"
      pattern: '"contextMenus"'
---

<objective>
Add foundation for context menu integration: permission declaration and storage key constant.

Purpose: Enable Chrome contextMenus API and provide storage key for captured image URLs.
Output: manifest.json with contextMenus permission, constants.ts with CAPTURED_IMAGE_STORAGE_KEY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-context-menu-foundation/09-CONTEXT.md
@.planning/phases/09-context-menu-foundation/09-RESEARCH.md
@.planning/phases/09-context-menu-foundation/09-PATTERNS.md

<interfaces>
<!-- Key patterns executor needs from existing code -->

From manifest.json (line 35):
```json
"permissions": ["activeTab", "downloads", "storage", "tabs", "alarms"]
```

From src/shared/constants.ts (line 10-18):
```typescript
export const STORAGE_KEY = 'prompt_script_data'
export const SYNC_DB_NAME = 'oh-my-prompt-sync'
export const SYNC_STORE_NAME = 'handles'
export const SYNC_HANDLE_KEY = 'syncFolderHandle'
```

Pattern: Underscore prefix for internal/transient storage keys (e.g., `_capturedImageUrl`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add contextMenus permission to manifest.json</name>
  <files>manifest.json</files>
  <read_first>
    - manifest.json (see current permissions array at line 35)
  </read_first>
  <action>
Per D-01 and D-06 from CONTEXT.md, add "contextMenus" to the permissions array.

Current permissions array (line 35):
```json
"permissions": ["activeTab", "downloads", "storage", "tabs", "alarms"]
```

Change to:
```json
"permissions": ["activeTab", "downloads", "storage", "tabs", "alarms", "contextMenus"]
```

Append "contextMenus" at the end of the array. Do NOT reorder existing permissions.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - manifest.json line 35 contains '"contextMenus"' in permissions array
    - All existing permissions remain unchanged
    - TypeScript compilation succeeds (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>
    manifest.json has "contextMenus" permission declared, enabling Chrome Context Menus API for Phase 9.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add CAPTURED_IMAGE_STORAGE_KEY constant to constants.ts</name>
  <files>src/shared/constants.ts</files>
  <read_first>
    - src/shared/constants.ts (see existing constant patterns and underscore naming convention)
  </read_first>
  <action>
Per D-05 and D-08 from CONTEXT.md, add a storage key constant for captured image URLs.

Add after line 31 (after ALLOWED_IMAGE_EXTENSIONS), before the closing of the file:
```typescript
// Captured image URL storage key (for Phase 9 context menu)
export const CAPTURED_IMAGE_STORAGE_KEY = '_capturedImageUrl'
```

Key naming rationale:
- Underscore prefix `_capturedImageUrl` follows existing pattern for internal/transient keys (see SYNC_HANDLE_KEY pattern)
- Not part of StorageSchema to reduce coupling per D-08
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/shared/constants.ts contains "export const CAPTURED_IMAGE_STORAGE_KEY = '_capturedImageUrl'"
    - TypeScript compilation succeeds (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>
    CAPTURED_IMAGE_STORAGE_KEY constant exported, ready for service-worker.ts import in Plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest.json → Chrome | Permission declaration grants API access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | manifest permission | accept | Permission only grants API access, no data exposure |
| T-09-02 | Tampering | constants.ts | accept | Constant value is static, not user-configurable |
</threat_model>

<verification>
1. TypeScript type check passes: `npx tsc --noEmit`
2. manifest.json contains "contextMenus" in permissions
3. constants.ts exports CAPTURED_IMAGE_STORAGE_KEY
</verification>

<success_criteria>
- manifest.json permissions array includes "contextMenus"
- CAPTURED_IMAGE_STORAGE_KEY constant exists in constants.ts
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-context-menu-foundation/09-01-SUMMARY.md`
</output>