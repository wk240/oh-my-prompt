---
wave: 1
depends_on: []
files_modified:
  - manifest.json
autonomous: true
requirements_addressed: [EXT-01, EXT-04]
---

# Plan 02: Manifest V3 Configuration

**Objective:** Create Chrome Extension manifest.json with Manifest V3 format, defining Service Worker, Content Script, and Popup.

---

## Task 01-02-01: Create manifest.json

<read_first>
- No existing file (new project)
- .planning/CONTEXT.md D-01 for Lovart domain match pattern
- .planning/RESEARCH.md section 1 for Manifest V3 structure
</read_first>

<action>
Create `manifest.json` with exact values:
- manifest_version: 3
- name: "Lovart Prompt Injector"
- version: "1.0.0"
- description: "一键插入预设提示词，提升Lovart平台创作效率"
- background.service_worker: "src/background/service-worker.ts"
- background.type: "module"
- content_scripts: [{ matches: ["*://*.lovart.ai/*"], js: ["src/content/content-script.ts"], run_at: "document_idle" }]
- action.default_popup: "src/popup/popup.html"
- action.default_icon: { "16": "assets/icon-16.png", "48": "assets/icon-48.png", "128": "assets/icon-128.png" }
- permissions: ["activeTab"]
- icons: { "16": "assets/icon-16.png", "48": "assets/icon-48.png", "128": "assets/icon-128.png" }
</action>

<acceptance_criteria>
- manifest.json exists at project root
- manifest.json contains "manifest_version": 3
- manifest.json contains "background" with "service_worker"
- manifest.json contains "content_scripts" with matches: ["*://*.lovart.ai/*"]
- manifest.json contains "action" with "default_popup"
- manifest.json contains "permissions": ["activeTab"]
</acceptance_criteria>

---

## Verification

<manual>
After completing task:
1. Verify: manifest.json is valid JSON (parse with JSON.parse)
2. Verify: manifest_version is exactly 3 (not 2)
3. Verify: content_scripts matches pattern includes "*.lovart.ai/*"
</manual>

---

## Threat Model

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Overly broad content script matches | LOW | Matches limited to `*://*.lovart.ai/*` per D-01, not all URLs |
| Excessive permissions | LOW | Only "activeTab" requested, no "tabs" or "storage" yet |
</threat_model>

---

*Plan 02 for Phase 01-foundation-manifest-setup*