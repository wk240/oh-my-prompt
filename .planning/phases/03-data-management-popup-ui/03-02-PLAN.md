---
wave: 1
depends_on: [03-01]
files_modified:
  - src/background/service-worker.ts
autonomous: true
---

# 03-02: Service Worker Storage Handlers

## Requirements
- DATA-01: 提示词数据本地持久化存储

## Summary
Implement GET_STORAGE and SET_STORAGE message handlers in the Service Worker to enable Content Script and Popup communication with the Storage Manager. This completes the message protocol established in Phase 1.

## Tasks

### Task 1: Implement Storage Message Handlers
<read_first>
- src/background/service-worker.ts: Current message handlers with placeholder GET_STORAGE/SET_STORAGE
- src/shared/messages.ts: MessageType enum and Message/MessageResponse interfaces
- src/lib/storage.ts: StorageManager class from Plan 03-01
</read_first>

<action>
Update `src/background/service-worker.ts`:

1. Import StorageManager:
   - Add `import { StorageManager } from '../lib/storage'` at top

2. Create StorageManager instance:
   - Add `const storageManager = new StorageManager()` after console.log

3. Implement GET_STORAGE handler (replace placeholder):
   ```typescript
   case MessageType.GET_STORAGE:
     storageManager.getData()
       .then(data => sendResponse({ success: true, data } as MessageResponse<StorageSchema>))
       .catch(error => {
         console.error('[Lovart Injector] GET_STORAGE error:', error)
         sendResponse({ success: false, error: 'Storage retrieval failed' })
       })
     return true // Required for async response
   ```

4. Implement SET_STORAGE handler (replace placeholder):
   ```typescript
   case MessageType.SET_STORAGE:
     if (!message.payload) {
       sendResponse({ success: false, error: 'No payload provided' })
       return true
     }
     storageManager.saveData(message.payload as StorageSchema)
       .then(() => sendResponse({ success: true } as MessageResponse))
       .catch(error => {
         console.error('[Lovart Injector] SET_STORAGE error:', error)
         sendResponse({ success: false, error: 'Storage save failed' })
       })
     return true // Required for async response
   ```

5. Import StorageSchema type for proper typing:
   - Add `import type { StorageSchema } from '../shared/types'`
</action>

<acceptance_criteria>
- src/background/service-worker.ts imports StorageManager from '../lib/storage'
- src/background/service-worker.ts imports StorageSchema type
- GET_STORAGE handler calls storageManager.getData() and returns StorageSchema
- SET_STORAGE handler calls storageManager.saveData() with payload
- Both handlers use try-catch or Promise catch for error handling
- Both handlers return `true` for async sendResponse
- TypeScript compilation passes
</acceptance_criteria>

## Verification
1. Service Worker imports StorageManager
2. GET_STORAGE returns storage data via MessageResponse
3. SET_STORAGE saves payload and returns success
4. TypeScript compiles without errors

## must_haves
- GET_STORAGE handler implemented with async storage retrieval
- SET_STORAGE handler implemented with async storage save
- Proper error handling with MessageResponse format
- Import of StorageManager and StorageSchema types